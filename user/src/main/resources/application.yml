server:
  port: 8801
spring:
  application:
    name: UserServer
  profiles:
    active: dev
  config:
    import: "optional:nacos:${spring.application.name}-${spring.profiles.active}.properties"
  nacos:
    username: nacos
    password: nacos
    config:
      server-addr: 127.0.0.1:8848
    discovery:
      server-addr: 127.0.0.1:8848
feign:
  sentinel:
    enabled: true